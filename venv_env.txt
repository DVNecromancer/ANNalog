# requirements-venv-cu111.txt
# Target: Python 3.9.x on Linux
#
# Create venv with Python 3.9, then:
#   python -m pip install -U pip setuptools wheel
#   python -m pip install -r requirements-venv-cu111.txt
#
# NOTE: torch/torchvision CUDA 11.1 wheels are hosted on the PyTorch wheel index (not plain PyPI).

--find-links https://download.pytorch.org/whl/torch_stable.html

# PyTorch (CUDA 11.1)
torch==1.9.1+cu111
torchvision==0.10.1+cu111

# Core deps (from your conda YAML, build strings removed)
numpy==1.20.3
pandas==1.3.3
tqdm==4.60.0
scikit-learn==1.2.2

# RDKit: conda 2021.09.4 corresponds to rdkit-pypi 2021.9.4 on PyPI
rdkit-pypi==2021.9.4

# If you DON'T have a CUDA 11.1-compatible setup, use CPU wheels instead:
# --find-links https://download.pytorch.org/whl/torch_stable.html
# torch==1.9.1+cpu
# torchvision==0.10.1+cpu